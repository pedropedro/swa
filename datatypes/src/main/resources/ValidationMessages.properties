# How to use conditional message formatting depending on an annotation value:
invalid.range=value ${context.isEmpty() ? '' : 'of the '.concat(context).concat(' ')}must be within range {min} to {max}

invalid.time-interval=start time point must precede the end
invalid.email=address not valid
